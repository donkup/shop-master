import { CartToCartMapPipe } from './cart-to-cart-map.pipe';

describe('CartToCartMapPipe', () => {
  it('create an instance', () => {
    const pipe = new CartToCartMapPipe();
    expect(pipe).toBeTruthy();
  });
});
